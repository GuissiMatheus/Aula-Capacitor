import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nomequalquer.app',
  appName: 'aula_capacitor',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
